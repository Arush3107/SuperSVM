function v = feature_transformation(u)
%This method is used to transform the input data which intakes it as input
%and provides the transformed data as the output. For simplicity we are
%using the exact data but non-linear transformation can applied for better
%results.
    v = u;
end